 <div class="container">
 <div class="row">
 <div class="col-md-8" style="background-color: ">

<h3>商品一覧</h3>

<%= link_to "新規登録　", new_admin_item_path %>

<%= form_with(model: @items, url: admin_items_path, method: :get) do |f| %>
 <div class="col-md-8 offset-md-1">

 <table class="table">
  <thead>
    <th>商品ID</th>
    <th>商品名</th>
    <th>単価(税込)</th>

  </tbody>
  </thead>


<% @items.each do |item| %>
 <table class="table">
  <th><%= item.id %></th>

  <th> <%= link_to admin_item_path(item) do %>
    <%= item.name %></th>
   <% end %>
  <!--<th><%#= item.introduction %></th>-->
  <th><%= item.price %></th>
  <!--<th>ジャンル：</th>-->
  <!--<th>ステータス：</th>-->
<th><%#= image_tag item.get_image %></th>
<% end %>

<% end %>

</div>
</div>
