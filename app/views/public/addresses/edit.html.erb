<h1>配送先編集</h1>
<%= form_with model: @address do |f| %>

  <h4>郵便番号</h4>
  <%= f.text_field :postal_code %>
  <h4>住所</h4>
  <%= f.text_area :address %>

  <h4>氏名</h4>
  <%= f.text_area :name %>

  <%= f.submit '内容変更を保存' %>

<% end %>
