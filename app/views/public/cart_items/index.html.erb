<div class="container">
 <div class="row">
 <div class="col-md-8" style="background-color: ">

<h3>ショッピングカート</h3>
<td><%= link_to "カートを空にする", cart_items_destroy_all_path, method: :delete, "data-confirm" => "本当に削除しますか？" %></td>


 <table class="table">
  <thead>
    <th>商品名</th>
    <th>単価(税込)</th>
    <th>数量</th>
    <th>小計</th>



  </thead>
  <tbody>
  <% @cart_items.each do |cart_item| %>
   <div class="col-md-8 offset-md-1">

      <tr>

      <tb>
        <%= form_with model: cart_item, url:cart_item_path(cart_item.id) do |f| %>
      <tr>
             <%#= image_tag item.get_image %>
       <p>
       <%#= image_tag @cat_item.get_image %>
      </p>
      <%= image_tag cart_item.item.get_image %>

      <td><%= cart_item.item.name%></td>

      <td><%= cart_item.item.with_tax_price%></td>

      <td><%= f.select :amount, *[1..20] %><%= f.submit "変更" %></td>
     <!--<%#= cart_item.amount%>  -->
      <!--<td><%#= f.select :amount, *[1..20] %></td>-->
      <!--<td><%#= f.submit "変更" %></td>-->

      <td><%= cart_item.subtotal%></td>
      <td><% @sum += (cart_item.item.price * 1.1).floor * cart_item.amount %></td>
      <td><%= link_to "削除", cart_item_path(cart_item.id), method: :delete, "data-confirm" => "本当に削除しますか？" %></td>


      <td><%#= link_to '削除', cart_item_path(cart_item), method: :delete %></td>

      <% end %>
      </tb>
    </tr>
  <% end %>

      <table class="table">
      <p>合計金額: <%= @sum %></p>



    <td><%= link_to "買い物を続ける", root_path %></td>
    <td><%= link_to "情報入力に進む", new_order_path %></td>
  </tbody>
</table>


 </div>
 </li>
</ul>


